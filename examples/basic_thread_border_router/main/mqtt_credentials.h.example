/*
 * SPDX-FileCopyrightText: 2026 Espressif Systems (Shanghai) CO LTD
 *
 * SPDX-License-Identifier: Apache-2.0
 */
#pragma once

// MQTT Configuration Template
// Copy this file to mqtt_credentials.h and fill in your actual credentials

#define MQTT_BROKER_URI     "mqtt://broker.example.com:1883"
#define MQTT_CLIENT_ID      "esp-ot-br"
#define MQTT_USERNAME       NULL  // Set to "your_username" or NULL if not required
#define MQTT_PASSWORD       NULL  // Set to "your_password" or NULL if not required
#define MQTT_BASE_TOPIC     "esp-ot-br"
#define MQTT_UDP_PORT       12345
#define MQTT_ML_EID_REQUEST_TIMEOUT_MS  5000  // Timeout for ML-EID backend query (milliseconds)
#define MQTT_MAX_REGISTERED_DEVICES  512  // Maximum number of devices in ML-EID registry
